# URL base da API (obrigatório em produção na Vercel)
# Desenvolvimento: não defina ou deixe vazio para usar o proxy /api → localhost:5053
# Produção: URL do backend publicado (ex: Railway, Azure, Render)
#
# Exemplos:
# VITE_API_URL=https://nexus-med-api.railway.app/api
# VITE_API_URL=https://nexus-med-api.azurewebsites.net/api
# VITE_API_URL=https://nexus-med-api.onrender.com/api
#
VITE_API_URL=
